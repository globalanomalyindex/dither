{
  "name": "@globalanomalyindex/dither",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first browser image transformation workshop.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.9.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "vite --host 127.0.0.1",
    "format": "prettier --write scripts src tests *.config.mjs package.json tsconfig.json .prettierrc.json",
    "format:check": "prettier --check scripts src tests *.config.mjs package.json tsconfig.json .prettierrc.json",
    "inspect:algorithms": "node scripts/inspect-dither-registry.mjs",
    "lint": "eslint scripts tests *.config.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "npm run test:unit",
    "test:e2e": "playwright test",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run lint && npm run format:check && npm run test:unit && npm run build"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.3",
    "@playwright/test": "1.61.1",
    "eslint": "10.7.0",
    "prettier": "3.9.4",
    "typescript": "6.0.3",
    "vite": "8.1.4",
    "vitest": "4.1.10"
  }
}
